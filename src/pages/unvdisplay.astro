---
import MainLayout from "@/layouts/MainLayout.astro";
import { unvdisplaydata } from "../data/camera";
import { getCollection } from "astro:content";
const pageTitle: string = "UNV Smart Interactive Display";
interface Props {
  title: string;
  subTitle?: string;
  benefits?: Array<string>;
}

interface Card {
  id: number;
  title: string;
  desc: string;
  img: string;
  alt: string;
  author: string;
  reviewBody: string;
  ratingValue: number;
  slug: string;
  features: any;
  publishedTime: string;
}

const content = await getCollection("unvdisplay");
const cards: Card[] = content.map((item) => ({
  id: item.data.id,
  title: item.data.title,
  desc: item.data.desc,
  img: item.data.img.src,
  alt: item.data.alt,
  author: item.data.author,
  reviewBody: item.data.reviewBody,
  ratingValue: item.data.ratingValue,
  slug: item.slug,
  features: item.data.features,
  publishedTime: item.data.publishedTime,
}));
const description =
  "Find the best touch screen board for teaching price! Lovosis Technology LLC, Dubai, offers UNV Smart Interactive Panels & LCD video walls. Transform your classroom today!";
const abtitle: string = "UNV Smart Interactive Display";
const absubTitle: string =
  "Experience the future of interactive learning with our UNV smart interactive display models";
const canonicalUrl = new URL("/unidisplay", Astro.site).toString();
const metaTags = [
  // Basic SEO
  { name: "title", content: pageTitle },
  { name: "description", content: description },
  {
    name: "keywords",
    content:
      "Uniarch Dubai Price, Bulk CCTV UAE, Commercial Security Dubai, AI Surveillance UAE, Night Vision Cameras Dubai, Access Control Systems, Wholesale Security Middle East",
  },
  { name: "robots", content: "index, follow" },

  // Open Graph/Facebook

  // Geo Tags
  { name: "geo.region", content: "AE-DU" },
  { name: "geo.placename", content: "Dubai" },
  { name: "geo.position", content: "25.276987;55.296249" },
  { name: "ICBM", content: "25.276987, 55.296249" },

  // Canonical
  { name: "canonical", content: `${Astro.url.origin}/unvdisplay` },

  // Contact Information
  { name: "contact", content: "sales@lovosis.com" },
  { name: "telephone", content: "+971509162488" },

  // Publisher Information
  { name: "author", content: "Lovosis Technology L.L.C" },
  { name: "publisher", content: "Lovosis Technology L.L.C" },
];
const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Organization",
      name: "Lovosis Technology L.L.C",
      url: "https://lovosis.com",
      logo: "https://lovosis.come.com/pictures/logo.png",
      description: description,
      sameAs: [
        "https://www.facebook.com/p/LOVOSIS-TECHNOLOGY-100083060600975/",
        "https://www.instagram.com/_lovosis_group_of_companies_/reels/",
      ],
      email: "sales@lovosis.com",
      telephone: "+971509162488",
      address: {
        "@type": "PostalAddress",
        streetAddress: "Satellite Market Naif Deira,Baghlaf Building Showroom No.5",
        addressLocality: "Dubai",
        addressCountry: "AE",
        addressRegion: "Dubai,United Arab Emirates",
        postalCode: "123421",
      },
    },
    {
      "@type": "WebPage",
      name: "Unv Interactive panel | Lovosis Technology",
      url: "https://lovosis.com/unvdisplay",
      description: description,
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://lovosis.com/#breadcrumb",
      itemListElement: [
        {
          "@type": "ListItem",
          position: 1,
          item: {
            "@id": "https://lovosis.com",
            name: "Home",
          },
        },
        {
          "@type": "ListItem",
          position: 2,
          item: {
            "@id": "https://lovosis.com/unvdisplay",
            name: "Unv Display",
          },
        },
      ],
    },
    // First Product List (UNV Displays)
   
    // Second Product List (Another Category)
     {
      "@type": "ItemList",
      itemListElement: cards.map((product, index) => ({
        "@type": "ListItem",
        position: index + 1,
        item: {
          "@type": "Product",
          name: product.title,
          description: product.desc,
          image: new URL(product.img, Astro.site).toString(),
         
          url: new URL(`/unvdisplay/${product.slug}`, Astro.site).toString(),
          offers: {
            "@type": "Offer",
            priceCurrency: "AED",
            price: "0.00",
            availability: "https://schema.org/InStock",
            itemCondition: "https://schema.org/NewCondition",
          },
          aggregateRating: {
            "@type": "AggregateRating",
            ratingValue: product.ratingValue.toString() || "4.5",
            reviewCount: "10",
          },
          brand: {
            "@type": "Brand",
            name: "UNV Display",
          },
        },
      })),
    },
  ],
};
---

<style>
  .autoRotate {
    animation: autoRotateAnimation;
    animation-timeline: view();
  }
  @keyframes autoRotateAnimation {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .autoShow {
    animation: autoShowAnimation both;
    animation-timeline: view(70% 5%);
  }

  @keyframes autoShowAnimation {
    from {
      opacity: 0;
      transform: translateY(200px) scale(0.3);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .fadeInRight {
    animation: fadeInRightAnimation;
    animation-timeline: view();
  }

  @keyframes fadeInRightAnimation {
    from {
      opacity: 0;
      transform: translateX(100px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
</style>

<MainLayout
  title={pageTitle}
  sd={structuredData}
  metaTags={metaTags}
  description={description}
  canonical={canonicalUrl}
>
  <section
    class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
  >
    <div
      class="mb-20 rounded-2xl bg-gray-100 p-5 text-center dark:bg-neutral-500 md:p-12"
    >
      <h1
        class="animate-fade-in-left text-balance text-3xl font-bold tracking-tight text-neutral-800 dark:text-neutral-100 md:text-6xl md:leading-tight"
      >
        {abtitle}
      </h1>
      <p class="mt-1 text-pretty text-neutral-600 dark:text-neutral-200">
        {absubTitle}
      </p>
    </div>
    <div class="grid grid-cols-2 gap-4 md:grid-cols-4">
      <div class="bg-white shadow-xl dark:bg-neutral-500">
        <img
          src="/pictures/Unvdisplay/Unvdisplay.png"
          ,
          alt="interactive display"
          class="h-full w-full object-contain"
        />
        <p class="mt-4 text-center text-2xl font-bold dark:text-white">
          MW3565-U-E2
        </p>
      </div>
      <div class="bg-white shadow-xl dark:bg-neutral-500">
        <img
          src="/pictures/Unvdisplay/MW2.png"
          ,
          alt="interactive panel"
          class="h-full w-full object-contain"
        />
        <p
          class="mt-4 text-center text-2xl font-bold text-black dark:text-white"
        >
          MW7815-FI-EB
        </p>
      </div>
      <div class="bg-white shadow-xl dark:bg-neutral-500">
        <img
          src="/pictures/Unvdisplay/MW1.png"
          ,
          alt="Lcd walls"
          class="h-full w-full object-contain"
        />
        <p class="mt-4 text-center text-2xl font-bold dark:text-white">
          MW7809-FI-CF
        </p>
      </div>
      <div class="bg-white shadow-xl dark:bg-neutral-500">
        <img
          src="/pictures/Unvdisplay/Unvdisplay8.png"
          ,
          alt="Led display"
          class="h-full w-full object-contain"
        />
        <p class="mt-4 text-center text-2xl font-bold dark:text-white">
          MW-A46-B1-E
        </p>
      </div>
    </div>
    <div class="mb-5 mt-32 rounded-2xl text-center">
      <h2
        class="animate-fade-in text-2xl font-semibold text-gray-900 dark:text-gray-200"
      >
        Lovosis Technology LLC, Dubai, is the authorized distributor of UNV
        Smart Interactive Panels
      </h2>
    </div>
    <div class="mx-auto max-w-7xl text-gray-500 dark:text-gray-200">
      <p class="animate-slide-in-left delay-100">
        <strong>UNV Smart Interactive Panels</strong> are designed to transform the
        way you teach, learn, and collaborate. With advanced features like ultra-responsive
        touch screen technology, crystal-clear interactive display, and seamless
        integration with modern teaching tools, <strong>UNV panels</strong> are the
        ultimate solution for educators and professionals alike.
      </p>
    </div>

    <div class="mb-5 mt-16 rounded-2xl text-center">
      <h2
        class="animate-fade-in text-2xl font-semibold text-gray-900 dark:text-gray-200"
      >
        Why Choose UNV Smart Interactive Panels?
      </h2>
    </div>
    <div class="mx-auto max-w-7xl text-gray-500 dark:text-gray-200">
      <p class="animate-slide-in-left delay-100">
        <strong>1: High-Resolution Interactive Display:</strong>
      </p>
      <li class="animate-slide-in-left delay-200">
        Enjoy stunning visuals with 4K UHD resolution, perfect for engaging
        students or presenting detailed data
      </li>
      <p class="animate-slide-in-left delay-300">
        <strong>2: Multi-Touch Capability</strong>
      </p>
      <li class="animate-slide-in-left delay-400">
        Up to 20-point touch support for seamless collaboration and interaction.
      </li>
      <p class="animate-slide-in-left delay-500">
        <strong>3: Built-In Android and Windows Compatibility</strong>
      </p>
      <li class="animate-slide-in-left delay-600">
        Easily switch between platforms for maximum flexibility.
      </li>
      <p class="animate-slide-in-left delay-700">
        <strong>4: Long-Lasting Durability</strong>
      </p>
      <li class="animate-slide-in-left delay-800">
        Designed for heavy daily use, ensuring reliability for years to come
      </li>
      <p class="animate-slide-in-left delay-900">
        <strong>5: Easy-to-Use Interface</strong>
      </p>
      <li class="animate-slide-in-left delay-1000">
        Intuitive controls make it simple for teachers and presenters to get
        started.
      </li>
    </div>

    <style>
      /* Animation classes */
      .animate-fade-in {
        opacity: 0;
        transition: opacity 0.6s ease-out;
      }

      .animate-slide-in-left {
        opacity: 0;
        transform: translateX(-20px);
        transition:
          opacity 0.6s ease-out,
          transform 0.6s ease-out;
      }

      /* Delay classes */
      .delay-100 {
        transition-delay: 100ms;
      }
      .delay-200 {
        transition-delay: 200ms;
      }
      .delay-300 {
        transition-delay: 300ms;
      }
      .delay-400 {
        transition-delay: 400ms;
      }
      .delay-500 {
        transition-delay: 500ms;
      }
      .delay-600 {
        transition-delay: 600ms;
      }
      .delay-700 {
        transition-delay: 700ms;
      }
      .delay-800 {
        transition-delay: 800ms;
      }
      .delay-900 {
        transition-delay: 900ms;
      }
      .delay-1000 {
        transition-delay: 1000ms;
      }

      /* When element is in view */
      .in-view {
        opacity: 1;
        transform: translateX(0);
      }
    </style>

    <script>
      // Intersection Observer setup
      document.addEventListener("DOMContentLoaded", function () {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add("in-view");
                // Optional: Unobserve after animation
                observer.unobserve(entry.target);
              }
            });
          },
          {
            threshold: 0.1,
          }
        );

        // Observe all animated elements
        document
          .querySelectorAll(".animate-fade-in, .animate-slide-in-left")
          .forEach((el) => {
            observer.observe(el);
          });
      });
    </script>
    <div class="mx-auto mt-20 max-w-7xl px-4 sm:px-6 lg:px-8 lg:py-1">
      <h2 class="text-2xl font-semibold text-gray-900 dark:text-gray-200">
        <div class="mb-5 mt-16 rounded-2xl text-center">
          Latest Products from Lovosis Technology
        </div>
      </h2>
      <ul id="cards" class="grid list-none grid-cols-1 grid-rows-4 gap-4">
        {
          unvdisplaydata.map((item) => (
            <li class="card sticky top-32 cursor-pointer rounded-lg bg-white pt-4 shadow-xl transition-all duration-300 dark:bg-neutral-600">
              <div class="card__content relative grid grid-cols-1 gap-1 rounded-lg p-5 text-left md:grid-cols-2">
                <div>
                  <h2 class="text-4xl font-light dark:text-neutral-200">
                    {item.title}
                  </h2>
                  <p class="mt-3 text-base font-light leading-normal text-gray-500 dark:text-neutral-300">
                    {item.description}
                  </p>
                </div>
                <div class="self-end">
                  <figure class="h-64 overflow-hidden">
                    <img
                      src={item.image}
                      alt={item.title}
                      class="h-full w-full object-contain"
                    />
                  </figure>
                </div>
              </div>
            </li>
          ))
        }
      </ul>
    </div>
    <div class="mb-5 mt-16 rounded-2xl text-center">
      <h2 class="text-2xl font-semibold text-gray-900 dark:text-gray-200">
        Why Lovosis Technology LLC?
      </h2>
    </div>
    <div class="mx-auto max-w-7xl text-gray-500 dark:text-gray-200">
      As the <strong
        >authorized distributor of UNV Smart Interactive Panels in Dubai</strong
      >, we pride ourselves on delivering top-quality products and exceptional
      customer service. Our team of experts is here to help you choose the right
      solution for your needs and provide ongoing support to ensure your
      satisfaction
    </div>
    <div class="mb-5 mt-16 rounded-2xl text-center">
      <h2 class="text-2xl font-semibold text-gray-900 dark:text-gray-200">
        Get Started Today!
      </h2>
    </div>
    <div class="mx-auto max-w-7xl text-gray-500 dark:text-gray-200">
      <p>
        Ready to take your teaching or presentations to the next level? Explore
        our range of interactive panels, smart boards, and LCD video walls
        today. Contact Lovosis Technology LLC for more information or to request
        a demo.
      </p>
<section class="mx-auto max-w-[1400px] px-4 pt-8">
  <div class="mx-auto mb-12 max-w-2xl">
    <h2 class="mb-6 text-center text-3xl font-bold text-black dark:text-gray-200">
      UNV Display Products
    </h2>
    <div class="relative">
      <input
        type="text"
        id="search"
        class="w-full rounded-lg border border-gray-300 px-4 py-3 pl-12 text-base shadow-sm transition-all duration-300 focus:border-blue-500 focus:outline-none focus:ring-1 focus:ring-blue-500"
        placeholder="Search Uniarch Products..."
      />
      <svg
        class="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-gray-400"
        xmlns="http://www.w3.org/2000/svg"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
      </svg>
    </div>
  </div>
  <section class="scroll-animate mx-auto max-w-[1400px] px-4 pb-8">
    <div></div>
    <section class="mx-auto max-w-[1400px] px-4 pb-8">
      <div class="grid grid-cols-1 gap-8 md:grid-cols-2 lg:grid-cols-4">
        {
          cards.map((card) => (
            <div class="transform overflow-hidden rounded-lg bg-white opacity-0 shadow-lg transition-all duration-500 ease-[cubic-bezier(0.25,0.1,0.25,1)] hover:shadow-xl dark:bg-neutral-500">
              <div class="flex items-center justify-center p-4">
                <img
                  src={card.img}
                  alt={card.alt}
                  class="product-image h-48 w-auto object-contain transition-transform duration-300 hover:scale-105"
                />
              </div>
              <div class="flex flex-col items-center p-6">
                    <h3 class="text-center text-xl font-semibold text-black dark:text-white">
                      {card.title}
                    </h3>
                    <p class="mb-4 mt-3 text-center text-gray-600 dark:text-gray-300">
                      {card.desc}
                    </p>
                    <a href={`/unvdisplay/${card.slug}`} class="read-more-btn">
                      Read More
                    </a>
                  </div>
            </div>
          ))
        }
      </div>
    </section>
  </section>
  <p
    id="no-results"
    class="mx-4 my-6 hidden rounded-lg bg-gray-50 py-8 text-center text-xl font-medium text-gray-600 shadow-inner transition-opacity duration-300 dark:bg-gray-700 dark:text-gray-300"
  >
    <span class="mb-2 block text-3xl">🔍</span>
    No matching results found
  </p>
</section>

<script>
  // Intersection Observer for scroll animations
  document.addEventListener("DOMContentLoaded", function () {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("in-view");
            
            const cards = entry.target.querySelectorAll(".grid > div");
            cards.forEach((card, index) => {
              (card as HTMLElement).style.transitionDelay = `${index * 75}ms`;
              card.classList.remove("opacity-0");
              (card as HTMLElement).style.transform = "translateY(0)";
            });
          }
        });
      },
      { threshold: 0.1 }
    );

    document.querySelectorAll(".scroll-animate").forEach((section) => {
      observer.observe(section);
    });

    // Client-side search functionality
    const searchInput = document.getElementById("search");
    const cards = document.querySelectorAll(".grid > div");
    const noResults = document.getElementById("no-results");
    
    searchInput?.addEventListener("input", (e) => {
      const target = e.target as HTMLInputElement | null;
      const searchTerm = target?.value?.toLowerCase() || "";
      let hasVisibleCards = false;
      
      cards.forEach((card) => {
        const title = card.querySelector("h3")?.textContent?.toLowerCase() || "";
        const desc = card.querySelector("p")?.textContent?.toLowerCase() || "";
        
        if (title.includes(searchTerm) || desc.includes(searchTerm)) {
          (card as HTMLElement).style.display = "block";
          hasVisibleCards = true;
          
          // Add a subtle animation when showing cards
          card.animate(
            [
              { opacity: 0, transform: "translateY(20px)" },
              { opacity: 1, transform: "translateY(0)" }
            ],
            { duration: 300, easing: "ease-out" }
          );
        } else {
          // Add a fade-out animation before hiding
          const animation = card.animate(
            [
              { opacity: 1, transform: "translateY(0)" },
              { opacity: 0, transform: "translateY(20px)" }
            ],
            { duration: 200, easing: "ease-in" }
          );
          animation.onfinish = () => {
            (card as HTMLElement).style.display = "none";
          };
        }
      });
      
      // Animate no results message
      if (noResults) {
        if (hasVisibleCards) {
          noResults.classList.add("hidden");
        } else {
          noResults.classList.remove("hidden");
          noResults.animate(
            [
              { opacity: 0, transform: "scale(0.95)" },
              { opacity: 1, transform: "scale(1)" }
            ],
            { duration: 300, easing: "ease-out" }
          );
        }
      }
    });
  });
</script>

<style>
  /* Improved smooth transitions */
  .scroll-animate {
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .grid > div {
    transition: 
      opacity 0.5s cubic-bezier(0.4, 0, 0.2, 1),
      transform 0.5s cubic-bezier(0.4, 0, 0.2, 1),
      box-shadow 0.3s ease;
    transform: translateY(20px);
  }

  /* Hover effects */
  .grid > div:hover {
    transform: translateY(-5px) !important;
    box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }

  /* Dark mode adjustments */
  .dark .grid > div:hover {
    box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.3), 0 10px 10px -5px rgba(0, 0, 0, 0.1);
  }
</style>

<style>
  .hero-section {
    @apply bg-cover bg-center py-16 text-center text-white;
    background-image: url("");
  }
  .hero-content {
    @apply bg-black bg-opacity-50 px-4 py-16;
  }
  /* Read More Button Styles */
  .read-more-btn {
    @apply relative transform overflow-hidden rounded-lg border-2 border-transparent bg-blue-600 px-6 py-3 font-bold text-white transition-all duration-300;
  }
  .read-more-btn:hover {
    @apply -translate-y-1 border-blue-600 bg-white text-blue-600 shadow-xl;
  }
  .read-more-btn:active {
    @apply scale-95;
  }
  .read-more-btn::before {
    @apply absolute left-0 top-0 -z-10 h-full w-full origin-left scale-x-0 bg-white transition-transform duration-300;
    content: "";
  }
  .read-more-btn:hover::before {
    @apply scale-x-100;
  }
</style>
<script>
  // Client-side search functionality
  const searchInput = document.getElementById("search");
  const cards = document.querySelectorAll(".grid > div");
  const noResults = document.getElementById("no-results");
  searchInput?.addEventListener("input", (e) => {
    const searchTerm = (e.target as HTMLInputElement).value.toLowerCase();
    let hasVisibleCards = false;
    cards.forEach((card) => {
      const title = card.querySelector("h3")?.textContent?.toLowerCase() || "";
      const desc = card.querySelector("p")?.textContent?.toLowerCase() || "";
      if (title.includes(searchTerm) || desc.includes(searchTerm)) {
        card.classList.remove("hidden");
        hasVisibleCards = true;
      } else {
        card.classList.add("hidden");
      }
    });
    // Toggle no results message
    if (noResults) {
      noResults.classList.toggle("hidden", hasVisibleCards);
    }
  });
</script>
